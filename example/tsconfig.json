{
  "include": ["./src/**/*"],
  "compilerOptions": {
    "strict": true,
    "esModuleInterop": true,
    "lib": ["dom", "es2015"],
    "jsx": "react",
    "baseUrl": "./src/", // This must be specified if "paths" is.
    "paths": {
      "progress-ring": ["../../progress-ring"] // This mapping is relative to "baseUrl"
    }
  }
}
